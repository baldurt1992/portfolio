# npm 10.x Arborist crashes on Nuxt 4.5 peer resolution
# (TypeError: Cannot read properties of null (reading 'edgesOut')).
# Keep peer resolution relaxed until npm ships a stable fix for this graph.
legacy-peer-deps=true
